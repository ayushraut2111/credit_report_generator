how to setup a project :-
1. clone the repository
2. create a virtual environment
3. install requirements from requirements.txt
4. change directory to /report_generator
5. then run main.py file, and it will automatically take from sample_input and also output path is already define so it will also saved in the same directory also 
6. 
